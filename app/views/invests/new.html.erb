<% if current_user && current_user.auth_realname == 1 %>
	<%= form_for @bid, :url => invests_path do |f| %>
	  <%= render :partial => "form", :locals => {:f => f} %>
	<% end %>
<% else %>
	<%= link_to '请先进行实名认证', real_name_account_users_path, :style=>'color:#ff6666;' %>
<% end %>